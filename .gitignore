# dữ liệu, weight, kết quả không đưa vào git (quá lớn, up thủ công lên server)
weights/
# CHỈ thư mục data/ ở gốc repo (dataset thật), KHÔNG phải mọi thư mục tên data/.
# Không có dấu / đầu dòng thì rule này nuốt luôn package code
# count_editing/CE-LocModel/data/ (dataset.py, ce130_detection_dataset.py) ->
# server pull về sẽ thiếu và crash ngay ở `from data.dataset import ...`.
/data/
datasets/
output/
outputs/
*.pth
*.pkl

# python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# hệ điều hành / editor
.DS_Store
.idea/
.vscode/

# secret
.env
